# 名草 Bot 简介（给非技术同学看的）

名草 Bot 是一个部署在 QQ 上的多功能群聊机器人，目标是**帮群管理员减轻日常管理压力、帮群成员获得更好玩、更方便的聊天体验**。

下面的介绍不需要任何技术背景，只要你是群里的普通成员或管理员，都可以大致看明白 Bot 能做什么、怎么用。

---

## 名草 Bot 能做什么？

- **群功能开关管理**
  - 管理员可以通过指令查看本群有哪些功能可以用、当前开着还是关着。
  - 例如：自动欢迎新人、动态转发、聊天记录生成图片等，都可以按群单独开启/关闭。

- **聊天内容生成图片**
  - 将一段聊天转成类似 Twitter 引用风格的长图，方便截屏转发或保存。
  - 可以把 QQ 的聊天记录做成“聊天截图风格”的图片，更美观、适合分享。

- **Bilibili 动态转发**
  - 关注指定 UP 主的动态，当他们发新动态时，自动转发到群里。
  - 自动带上文字说明、封面图等，方便群友及时跟进。

- **举牌 / 表情图片生成**
  - 通过简单的指令生成“举牌”类整图表情，把你想说的话印在牌子上。
  - 适合日常“赞同 / 反驳 / 疑问”等氛围互动。

- **入群欢迎语**
  - 新人入群时自动发送欢迎消息。
  - 欢迎语支持 @ 新人、插入图片、倒计时（比如距离考试还有多少天）等。

- **自动/半自动入群审核**
  - 根据对方的验证信息自动判断是否通过入群申请（比如匹配姓名、关键字等）。
  - 不确定时会转给超级管理员，用私聊的方式“请示”是否同意。

- **学生生日播报**
  - 每天定时检查“今天是谁生日”，自动在开启了该功能的群里发送生日提醒。

- **帮助菜单**
  - 在支持的群里，可以通过 `/help` 查看本群已启用的功能列表和简单说明。

> 提示：具体有哪些功能可以用、哪些被关闭，取决于群管理员怎么配置。不同的群，体验会有所不同。

---

## 群成员大致怎么用？

下面的示例以 OneBot v11 适配的 QQ 群为主，不同部署方式可能略有差异，但整体体验类似。

### 1. 使用命令的常见习惯

大部分 Bot 功能通过**指令**触发，一般有三种形式：

- 在群里 `@名草Bot` 再加命令  
  例如：`@名草Bot help`
- 直接发送斜杠命令  
  例如：`/help`、`/miq`
- 直接发送中文指令  
  例如：`吃史`、`吃饭`、`投降`、`拼好饭` 等。

Bot 是否回应，取决于：

- 该功能有没有被管理员为本群开启；
- 你当前发送的格式是否正确；
- 当前平台（QQ群 / 私聊）是否支持该功能。

### 2. 常用交互举例

- **查看本群可用功能**
  - 在群内发送：`/help`
  - Bot 会以“合并转发”的形式发一条帮助消息，列出当前群已启用的功能。

- **把别人发的话做成“引用图片”**
  - 在群内找到你想生成图片的那条消息；
  - 回复那条消息，发送指令：`/miq`
  - Bot 会返回一张带头像、昵称、时间的图片，看起来类似 Twitter 引用截图。

- **生成聊天记录长图**
  - 对一条消息回复 `/miq 5`，表示以被回复的那条消息为结尾，向上取 5 条聊天记录，拼成一张图。
  - 适合“搬运一段聊天过程”的场景。

- **举牌类表情**
  - 在群里发送：`举牌 你说得对`  
  - 如果该群开启了“魔法举牌”功能，Bot 会返回一张举牌图片，上面写着“你说得对”。

- **自动入群审核 / 手动审核**
  - 对普通群成员来说，这个过程基本是“透明”的；
  - 有时你申请入群后会被机器人提示补充信息，或者管理员会看到 Bot 发来的“审核提醒”。

- **入群欢迎语与生日提醒**
  - 新人进群时 Bot 自动打招呼；
  - 每天早晨（或设定时间）Bot 可能会在群里发送“今天生日的学生名单”。

---

## 管理员大致怎么用？

管理员和超级用户可以通过为 Bot 提供的管理指令，对各个功能进行开关和配置。

### 1. 群内简单开关

在部分场景下，可以直接在群内使用中文指令：

- 开启某功能：`开启 功能名`
- 关闭某功能：`关闭 功能名`
- 查看本群功能状态：`本群功能`

这些命令仅对管理员 / 群主 / 超级用户开放，普通成员使用会被拒绝或忽略。

### 2. 统一管理入口：`/admin`

Bot 提供了一个更强大的管理插件，可以在**私聊**或**群聊**中使用 `/admin` 指令进行统一配置。

- 私聊中使用：
  - `/admin`：列出你在其中拥有管理员权限且 Bot 已加入的群；
  - `/admin <群号>`：查看该群已注册功能的开关状态；
  - `/admin <群号> <功能名> <on/off>`：开启或关闭对应功能。

- 群聊中使用：
  - `/admin`：查看当前群的功能状态；
  - `/admin <功能名> <on/off>`：在当前群开启或关闭功能。

> 实际可用指令以当前版本实现为准，Bot 会在响应消息中给出格式提示。

---

## 关于隐私与数据

名草 Bot 在运行过程中会存储少量必要数据，用于实现功能和风控：

- 群配置（哪些群启用了哪些功能）；
- 部分插件的数据（如入群规则、生日数据缓存、欢迎语配置等）；
- 极少量必要的日志，方便开发者排查错误。

Bot **不会主动收集或分析**与这些功能无关的个人隐私数据。  
如果你对具体数据存储位置或策略有疑问，可以联系 Bot 的维护者或查看面向技术人员的 `README.md`。

---

## 如果 Bot 不响应怎么办？

可能的原因包括：

- Bot 不在线或重启中；
- 该群未为某项功能开启权限；
- 指令格式有误（例如忘记 @Bot，或者参数拼写错误）；
- 当前适配器不支持某些特定功能。

你可以：

- 先试着在群内发送 `/help`，看看 Bot 是否在线、有无响应；
- 如果你是管理员，可以使用 `/admin` 或类似管理指令检查功能开关；
  - 如果你不是管理员，可以联系群管理员帮忙排查。

---

## 我适合看哪一个文档？

- 如果你只是**群成员或不写代码的管理员**，只看这一份 `introduction.md` 就足够了。
- 如果你打算**自己部署、二次开发或调试名草 Bot**，请同时查看项目根目录下的 `README.md`，里面有面向技术人员的详细说明。

